python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23

# PDF: o'qish (pypdf, pdfplumber) va yaratish (reportlab)
pypdf>=4.0.0
pdfplumber>=0.11.0
reportlab>=4.0.0

# OpenDocument (.odt/.ods/.odp) o'qish/yozish uchun
odfpy>=1.4.1

# Rasmdan matn o'qish (OCR) uchun — tizimda tesseract-ocr binary ham kerak
pytesseract>=0.3.10

# PDF sahifalarini rasmga aylantirish (convert_pdf_to_images, convert_pptx_to_images) uchun
pymupdf>=1.24.0

# Faqat rasm qo'shish funksiyalari (add_docx_image, add_pptx_image) uchun kerak
Pillow>=10.0.0

# Faqat example.py (Claude API demo) uchun kerak — asosiy toolkit uchun shart emas
anthropic>=0.40.0

# Faqat testlarni ishga tushirish uchun kerak (pytest tests/)
pytest>=8.0.0
